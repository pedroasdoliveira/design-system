import { Canvas, Meta, Unstyled } from '@storybook/blocks';
import { TokensGrid } from '../../components/TokensGrid';
import { fontSizes } from '@design-ui/tokens';

<Meta title="Tokens/Font-Sizes" />

# Font-sizes (Tamanho de Fontes)

Tamanho de fontes sendo utilizados.

<TokensGrid tokens={fontSizes} hasRemValue />