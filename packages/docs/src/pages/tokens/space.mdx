import { Canvas, Meta, Unstyled } from '@storybook/blocks';
import { TokensGrid } from '../../components/TokensGrid';
import { space } from '@design-ui/tokens';

<Meta title="Tokens/Space" />

# Spaces (Espaçamentos)

Espaçamentos sendo utilizados

<TokensGrid tokens={space} hasRemValue />